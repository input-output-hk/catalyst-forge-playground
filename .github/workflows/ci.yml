name: CI

on:
  push:

permissions:
  id-token: write
  contents: write
  packages: write
# temp
jobs:
  ci:
    uses: input-output-hk/catalyst-forge/.github/workflows/ci.yml@master
    with:
      forge_version: latest